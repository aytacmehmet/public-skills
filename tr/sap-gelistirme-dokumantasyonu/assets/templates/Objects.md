# ${title} — Object Inventory

Inventory coverage and baseline: not established. State how discovery was performed, where the source resides, and what is excluded. One owner per object; link reused objects to their shared component.

For each applicable group, use: package / object type / exact name / purpose / owner or reuse link / source or ADT reference / dependency / observed lifecycle state / evidence and time.

## Persistence and data model

Tables, structures, data elements, domains, and CDS entities as applicable.

## Behavior and application logic

RAP behavior definitions/implementations, classes, interfaces, functions, and BAdI implementations as applicable.

## Services and integration

Service definitions/bindings, communication artifacts, events, released interfaces, and external contracts.

## UI and navigation

UI5/Fiori components, views/controllers, annotations, app descriptors, catalogs, and navigation artifacts.

## Authorization and configuration

Authorization controls, role/catalog prerequisites, configuration dependencies, and technical settings.

## Tests and operations

Unit/integration tests, test doubles, diagnostics, jobs, and deployment/transport artifacts.

Do not mark a planned object as created or a local source as active in a tenant. Record evidence for each claimed lifecycle transition.
