# ${title} — History

Append meaningful events and linked corrections. Keep occurrence time separate from recording time. Event IDs are immutable and reusable only for the identical payload. Do not reconstruct unavailable history from guesses.
